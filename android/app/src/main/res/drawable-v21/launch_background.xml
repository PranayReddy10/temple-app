<?xml version="1.0" encoding="utf-8"?>
<!-- The launch window: plain temple-door teak and nothing else, so the
     first thing anyone sees is the doors (drawn by Flutter) in the same
     colour. No icon: showing one here meant a logo, then the doors. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background" />
</layer-list>
